console.log("the FOLDER containing the current file",__dirname);
console.log("the FULL PATH to the current file itself ",__filename);
console.log("an array with TWO items: the path to the node.exe executable itself, and the path to the script you ran",process.argv);